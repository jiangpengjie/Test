<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="perference_bg">
            <item name="android:windowBackground">@drawable/bg</item>  <!-- 窗口背景 -->
            <item name="android:focusable">false</item>
            <item name="android:textColor">@color/white</item> <!-- 字体颜色 -->
            <item name="android:colorBackgroundCacheHint">#00000000</item> <!-- 背景缓存色透明，避免拖动全黑 -->
            <item name="android:windowNoTitle">true</item> <!-- 是否有标题 -->
    </style>
</resources>
